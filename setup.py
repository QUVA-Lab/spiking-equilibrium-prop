from setuptools import setup, find_packages

setup(
    name = "spiking-equilibrium-prop",
    author = "peter",
    version = 0,
    dependency_links = (),
    install_requires = ['numpy', 'matplotlib'],
    scripts = [],
    packages=find_packages(),
    )
